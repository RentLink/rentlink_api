server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: root
    password: root
    url: ${DATABASE_URL}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    password: root
    user: root
    url: ${DATABASE_URL}
    schemas: rentlink

